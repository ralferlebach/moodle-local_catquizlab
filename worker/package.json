{
  "name": "catquizlab-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Puppeteer worker for local_catquizlab: plays simulated CAT attempts through the real mod_adaptivequiz UI. Triggered by timed ad-hoc tasks of the plugin; all simulation logic (response oracle) stays server-side.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node run_attempt.js",
    "check": "node --check run_attempt.js"
  },
  "dependencies": {
    "puppeteer": "^24.0.0"
  }
}
