define("local_catquizlab/livestatus",["exports","core/ajax"],(function(_exports,_ajax){var obj;
/**
   * Keep the operations view current while work is being done.
   *
   * The first version reloaded the whole page whenever the overall verdict
   * changed, which is the thing somebody watching a run notices most: the scroll
   * position goes, an open detail closes, and a form half filled in is gone.
   *
   * So the regions are patched in place, and a reload is kept for the one case
   * that cannot be patched — the page gaining or losing rows. Adding a run row
   * from here would mean a second renderer deciding what a run row looks like,
   * and two renderers disagree eventually.
   *
   * @module     local_catquizlab/livestatus
   * @copyright  2026 Ralf Erlebach
   * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
   */Object.defineProperty(_exports,"__esModule",{value:!0}),_exports.init=void 0,_ajax=(obj=_ajax)&&obj.__esModule?obj:{default:obj};let renderedShape=null,startedAt=0,timer=null;const setRegion=(region,value)=>{const node=document.querySelector('[data-region="'.concat(region,'"]'));node&&node.textContent!==String(value)&&(node.textContent=String(value))},poll=async()=>{if(!document.hidden)if(Date.now()-startedAt>18e5)stop();else try{const status=await _ajax.default.call([{methodname:"local_catquizlab_live_status",args:{}}])[0];setRegion("catquizlab-liveworkers",status.liveworkers),setRegion("catquizlab-queued",status.queued),setRegion("catquizlab-running",status.running),setRegion("catquizlab-collected",status.collected),setRegion("catquizlab-failed",status.failed),(status.regions||[]).forEach((region=>{setRegion(region.name,region.text)})),null===renderedShape?renderedShape=status.shape:renderedShape!==status.shape&&window.location.reload()}catch(error){stop()}},stop=()=>{null!==timer&&(window.clearInterval(timer),timer=null)};_exports.init=shape=>{renderedShape=shape||null,startedAt=Date.now(),stop(),timer=window.setInterval(poll,2e3),document.addEventListener("visibilitychange",(()=>{document.hidden||poll()}))}}));

//# sourceMappingURL=livestatus.min.js.map